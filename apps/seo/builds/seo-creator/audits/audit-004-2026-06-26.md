# Audit-004 — 2026-06-26 (the due audit before P1.R.3)

**Trigger:** 5 seo-creator runs since audit-003 → Phase 2 gate. **Scope:** Phase-1 (runs 015–019: P1.R.1/R.2/W.1/U.1/U.2/U.3/U.4) + the `@sagemark/imagegen` Stage 1 (#43) + Stage 2 (#45, 0035 applied to Sagemark + bucket). **4 auditors** (spec-completeness, test-quality, security, state-historian). **Mode:** read-only.

## VERDICT: ✅ NO CRITICAL — P1.R.3 CLEAR TO BUILD.
The governance moat (gate / FSM / `canPublish` / release+authorization fail-closed) is spec-complete and correct. MERGED-PR integrity 11/11 (all ancestors of origin/preview). Security posture holds: cross-tenant / publish-bypass / spend-safety / image-license-at-persist all PASS. Coverage strong (~958 tests; RLS Tier-2 now LIVE in CI, 19/19, 0 skipped; no hollow tests). Gaps are plumbing/bookkeeping + two Slice-1-close AC deviations + one publish-edge landmine for P1.R.3.

## Actionable (disposition)
| ID | Sev | Finding | Action |
|---|---|---|---|
| F1 | **High** | `/api/edit` has NO `status='draft'` guard — a review/approved/published piece can be silently edited (RFC PR-012 AC). | Corrective C.020.1 (add `status==='draft'` 409 before spend + a guards test) — do before/with P1.R.3. |
| F-LICENSE-1 | **High (landmine)** | No publish-side image-license gate (Never-list #8 enforced only at imagegen persist). Safe today by absence; **reopens when P1.R.3 wires `[photo:]`→image into published bodies.** | **[[DR-033]]** — `canPublish` precondition: every referenced image resolves to a licensed `generated_images`/stock row. MUST land with P1.R.3's resolution. |
| F3 | Med | DR-032 said Stage 2 "deferred" but #45 shipped + 0035 applied. | DISCHARGED — DR-032 Stage-2 addendum added. |
| F6 | Med | Version "switch" = `isActive` pointer flip, not "restore as a NEW auto-version" (RFC PR-013 AC); the test locks the divergence. | Spec-update the AC OR A.020.x restore-as-append — product sign-off. |
| F7 | Med | RFC/PRD still reference `packages/videogen/imagegen` (nonexistent); as-built is `@sagemark/imagegen` (DR-032). | Spec-update PRD §2/§3 + RFC §615/§94 → `@sagemark/imagegen`, cite DR-032. |
| F4 | Med | No `gate_results` writer — `canPublish` reads `eval_ran` but nothing writes it (PR-020/DR-025). Fail-closed safe. | spec-update DR-025/PR-020: "author the gate_results writer" = go-live prereq. |
| F5 | Med | No production `ContentDataAccess`/`PublicContentDataAccess` adapter (all throw `NOT_WIRED`). Documented, fail-closed. | umbrella DR/tracking: wire the live adapter + one real brief→…→publish→render smoke before Slice-1 "green". |
| C-1 | Med | **Migration-vs-live-pooled-role CI failure class recurred 3×** (C.008.1 PGOPTIONS, DR-015 rls_auto_enable EXECUTE, 0035 storage.buckets) — caught late, no structured check. | Wire judge check `migration-runs-on-live-pooled-role` (draft in this report). |
| C-1b / A.014.5 | Med | `tool-allowlist-single-source` + `worker-credential-publish-scope` (audit-002) + the normalize-before-gate lesson STILL unwired in `manifest.judge_criteria` after 3 cycles. | Promote all + the new migration check into the manifest before the next worker/schema PR. |
| C-2 | Med | `settings.local.json` `VERCEL_PROJECT_ID` pointed at the unused `sagemark` (prj_ZZ2O…), not the live `sagemark-seo` (prj_wd0r52t…). | **FIXED** (repointed to prj_wd0r52t this run). |
| F-RLS-1 | Low | `generated_images.client_id` nullable → dedup is workspace-wide (not per-client). | Document, or widen `insertAsset` + dedup on (ws,client,hash) if multi-client workspaces. DR-032 follow-up. |
| F2 | (status) | Worker has no audit/runGate bridge tool → can't self-gate. That's the unbuilt PR-014 full chain (#32 = writer-only). | Spec-status note; PR-014 adds it. |
| F8 | (P1.R.3 plan) | P1.R.3 trip-hazards: async/job-wrap the hero gen (don't block SSR); inject `makeNotWiredImageStore` (degrade to placeholder, not 500); render-gate off `license` presence; Pexels-first then generate. | Fold into the P1.R.3 plan. |
| F-EDIT-1 / F-RENDER-1 / F-3/4 / DR-020/030/031 carry | Low | edited-version needs a gate_results row to publish (fail-closed dead-end — confirm intended); EscapedHtml brand type; bridge-secret min-length + SSRF resolve-then-validate; intra-tenant replay; distributed rate-limiter; sign-off DB immutability. | opportunistic / phase-1-fold (in STATE active risks). |

## Positive confirmations
- **MERGED integrity:** 11/11 verified (#31/32/34/35/37/39/41 + #43/45 + state #42/44).
- **Security:** edit/versions/render reuse the same server-side tenancy chokepoint + fail-closed gate/FSM; imagegen refuses before spend, mints every license, fail-closed RLS (0035 anon→zero verified on Sagemark).
- **Tests:** no hollow/dishonest; RLS Tier-2 live 19/19; the spend-safety + sign-off-immutable + faithfulness-break-caught + revise-cap-terminates proofs are real.
- **Judge calibration healthy:** BLOCKED 0% all runs; re-judge non-zero only twice (both real defects caught). Process/product sustained 5.0.

## Draft structured check (C-1)
**`migration-runs-on-live-pooled-role`** — trigger: any PR touching `packages/schema-flywheel/drizzle/*.sql` or `rls-contract.test.ts`. Forced output: "list every schema this migration writes; for each non-`public` schema (storage/auth/extensions) + superuser-only construct (event triggers, SET ROLE, GRANT EXECUTE on system fns, ALTER OWNER), state whether the live pooled migration role can execute it; if any cannot → FAIL, require out-of-band or a documented external-provision note." Citation: 0035 storage.buckets → `0817379`; C.008.1; DR-015.

## Four auditors — summaries
1. **spec-completeness:** no anchor violation. Moat spec-complete. Gaps: F1 (edit status-guard, High), F-LICENSE-1, F4/F5 (stubbed pipeline, deferred), F6 (switch semantics), F3/F7/F9 (stale DR/path/migration-ref). P1.R.3 ready w/ F8 trip-hazards.
2. **test-quality:** STRONG, no hollow tests; true counts core 81 / imagegen 479 / seo 379(+1 skip) / RLS 19 live. A.020.1 (stale canvas Tier-3 comment + add a canvas interaction test), A.020.2 (promote ≥1 live smoke per now-runnable infra tier), A.020.3 (imagegen dedup orchestrator-path test).
3. **security:** posture holds; no exploitable hole. F-LICENSE-1 (Med, landmine → DR-033); F-RLS-1 (Low, ws-wide dedup); carry-overs F-1/F-2/DR-030/DR-031 re-confirmed open.
4. **state-historian:** MERGED 11/11; P1.R.3 ELIGIBLE NOW; decision-log gap (imagegen Stage 2, → DR-032 addendum); calibration healthy + C-1 recurring-migration class + C-1b unwired checks; C-2 VERCEL_PROJECT_ID (fixed).

*Consolidated by /seo-creator-build · audit-004 · 2026-06-26*
