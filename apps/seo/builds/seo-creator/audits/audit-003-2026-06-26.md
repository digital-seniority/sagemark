# Audit-003 — 2026-06-26 — PHASE-CLOSE (Phase 0→1 trust gate)

**Trigger:** Phase 0 — Foundations complete (10/10 PRs); required phase-boundary audit before Phase 1. **4 focused auditors** (spec-completeness, test-quality, security re-sweep, state-historian/DoD). **Mode:** read-only.

## VERDICT: ✅ CLEAR TO ENTER PHASE 1 — no Critical, no High exploitable finding.
Phase-0 DoD genuinely PASS (all 10 PRs MERGED + verified ancestors of origin/preview). Security posture **fail-closed** on cross-tenant / publish-bypass / capability-denial (all PASS, traced to enforcement lines). Test coverage GREEN (~729 tests in CI; no hollow/flaky/dishonest-skip; RLS Tier-2 17/17 executing). Decision-log complete (DR-001..025). Audit-002 carry-overs A.011.1/7/12/13 confirmed CLOSED.

## Actionable before / during Phase 1

| ID | Sev | Finding | Action |
|---|---|---|---|
| A.014.1 | **High** | **Funnel-stage enum drift:** golden `*.json` use `TOFU/MOFU/BOFU` but `0031` DB CHECK = `awareness/consideration/decision/retention` (+ PRD §3.5). A golden piece persisted as-is is CHECK-rejected; PR 017 groups by `funnel_stage`. | Normalize golden labels to the schema enum (TOFU→awareness, MOFU→consideration, BOFU→decision) + a test asserting every label satisfies the CHECK. Fold into P1.W.1/golden recalibration, **must precede PR 017**. |
| (seq) | — | **Slice-1 not yet end-to-end green:** Phase 0 delivers brief→draft→gate→persist→publish, but the Slice-1 DoD also needs the **minimal SSR render (PR 015 body-only)** + **one bounded edit→re-gate (PR 012 single edit)** — both Phase 1. | **Sequence Phase 1 to build the Slice-1 floor first** (P1.R.1 body-only render + the single bounded edit) before the full canvas (PR 010), per PRD §12 scope-inflation mitigation. |
| A.014.2 | Med | Golden corpus is 100% Stage-A-vetoed (all `VETO_BANNED_LEXICON`), so the clean Stage-B regression anchor is test-synthesized, not stored (DR-024). No stored clean baseline. | Capture ≥1 de-slopped clean piece into the corpus once the em-dash tension (A.014.4) is resolved. |
| A.014.3 | Med | Spec staleness: RFC §3.3 migration plan (`0033_seo_cost_ledger`) collides — `0033`/`0034` were consumed by RLS-fix + worker_sessions; PR 020's aux migration must renumber `0035+`. RFC §2 worker model seam still names `resolveGatewayModel('worker')` (DR-016 = env seam). | Spec-update RFC §3.3 + §2 (cite DR-016/DR-025). |
| A.014.4 | Med | **Cross-lane (content↔gate):** the canonical Whispering Willows reference demo fails the org's own banned-lexicon em-dash gate (all 10 pieces vetoed). Will throttle Slice-2 live drafting (formatting veto, not sourcing). **NON-ENG product-calibration call (James).** | Decide em-dash threshold vs de-slop the demo. |
| F-1 | Med | **Defense-in-depth:** the worker per-run bridge JWT can reach `/content/api/publish` (only the FSM release-gate blocks — sound today, the sole barrier). | Add an audience/scope claim to the bridge JWT (`["draft","audit","brief"]`); `/content/api/publish` rejects `via:"bridge"` (publish operator-only). Extends DR-018. |
| F-2 | Med | **Phase-1 landmine:** the auth seam (`getCurrentWorkspace`→null) fail-closes today, but when real auth lands the studio `/api/publish` has no operator-role authZ distinct from workspace resolution. | Before wiring real auth: add an operator-capability (who-may-publish) check + regression (401 no-session, 403 non-publisher). |
| A.014.5 | Med | The 2 drafted structured judge checks (`tool-allowlist-single-source`, `worker-credential-publish-scope`) + the "normalize-before-gate" lesson (DR-024) are prose, not wired into `manifest.judge_criteria`. | Promote to enforced judge_criteria before P1.W.1 (worker/golden work). |
| F-3/F-4 | Low | Bridge secret accepts any non-empty string (no min-length) + dual env-name fallback; SSRF guard is hostname-pattern (no resolve-then-validate / numeric-IP normalize — mitigated by VM subnet deny + 2KB untrusted-data handling). | Min-length assert + one canonical env name; resolve-then-validate IP. Opportunistic. |
| A.011.* carry | Low | A.011.4 (agent-worker/emit tests), A.011.5 (no-console), A.011.8 (schema-flywheel vacuous test), A.011.13 (alg check), event-log key-spacing drift. | Opportunistic; fold into touching PRs. |

## Triaged active-risk classes
- **Go-live blockers (worker undeployed):** live-Sandbox Tier-2/3 (skill load, adversarial re-run, SSE e2e, session round-trip — now RUNNABLE given the live Supabase branch + deployed host); `apps/seo/dist` worker build wiring (`build:worker`); expert golden-label certification (NON-ENG); the auth-seam operator-authZ (F-2) before real auth.
- **Phase-1-fold:** A.014.1 funnel enum (pre-PR017), gate_results table + projection widen (PR 020 / DR-025), DR-013 Gateway-only-metering (pre-PR020 ledger), PersistedAuthorization widen (P1.C.2), F-1 bridge audience claim, A.014.5 judge checks (pre-P1.W.1), DR-020 intra-tenant replay (when run registry exists).
- **Non-eng blockers:** D6 credentialed reviewer (P1.C.2 + golden cert), imagegen keys (P1.R.3), ≥3-engine SoM measurement (P1.C.4 — degraded v1 fallback specced).
- **Opportunistic:** A.011.4/5/8/13, SSRF hardening, event-log spacing.

## Phase-1 eligibility map (all Phase-0 deps MERGED)
**ELIGIBLE NOW (3, different lanes):** **P1.R.1** (PR 015 SSR render — highest leverage, gates P1.R.2/P1.C.3/P1.C.1), **P1.U.1** (PR 010 canvas shell), **P1.W.1** (PR 014 wire strategist/assistant/audit). Blocked intra-P1 or on non-eng: P1.U.2-4, P1.R.2 (←P1.R.1), P1.R.3 (←imagegen keys), P1.C.1-4 (←D6/SoM).

## Judge calibration (runs 001-013): healthy
Process 4.5-5.0 (no decline), product rising to sustained 5.0, BLOCKED 0% every run, re-judge 100% once (Run #012, a one-off — judge correctly caught the em-dash golden mask). "enforcement-seam-built-not-wired" theme converged (DR-018 standing CI regression). "normalize-before-gate" addressed as lesson + meta-check (promote to judge_criteria — A.014.5).

## Four auditors — summaries
1. **spec-completeness:** Phase-0 ship-gate table all MET (suite/golden PARTIAL = characterization not expert-cert). High F1 funnel enum; seq guidance (Slice-1 floor first); migration renumber; RFC §2 staleness. No anchor contradiction.
2. **test-quality:** GREEN, no hollow/flaky/dishonest-skip; ~729 CI tests; RLS Tier-2 17/17 executing. Med: no stored clean Stage-B golden anchor (DR-024 hole); AC6 adjudicateDispute is test-local. Tier-3 items 1-3 now runnable.
3. **security:** integrated Phase-0 surface fail-closed; cross-tenant/publish-bypass/capability-denial all PASS. Med F-1 (bridge audience claim) + F-2 (auth-seam landmine). A.011.7/12/13 closed. SSRF/secret-strength Low.
4. **state-historian:** Phase-0 DoD PASS (10/10 verified); event-log + PHASE_COMPLETE consistent; decision-log complete; triaged risks + Phase-1 map (above); A.014.5 judge-criteria-not-wired.

*Consolidated by /seo-creator-build · audit-003 phase-close · 2026-06-26*
