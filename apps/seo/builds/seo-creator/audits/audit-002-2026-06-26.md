# Audit-002 — 2026-06-26 (full, 5 parallel auditors)

**Trigger:** audit due before Run #011 (5 runs since audit-001). **Scope:** all merged work through Run #010 + the vendored suite (#24). **Mode:** read-only. **Headline:** **No Critical findings.** Build is fundamentally sound — module boundaries clean, no hollow/flaky tests, MERGED-PR integrity 9/9, event log reconciles, cross-tenant tenancy fully closed, audit-001 RLS Tier-2 orphan **CLOSED** (17/17 executing in CI). Real items are confinement/hardening + spec-reconciliation, no production-impacting defect.

## Disposition (audit consolidation rules: Critical→A.011.x PR human-merge; High→active risk; Med/Low→log)

No Criticals ⇒ no forced human-merge A-PRs. Highs tracked as active risks + addressed as correctives (these were self-flagged by judges / are spec reconciliations, not judge-missed Critical, so they may land as normal C.011.x / spec-updates).

| ID | Sev | Finding | Vehicle |
|---|---|---|---|
| A.011.1 | **High** | `agent-worker.ts:279-282` hardcodes model `allowedTools` literals instead of importing `WORKER_ALLOWED_TOOLS`; boot gate verifies list A, model runs list B — "single source of truth" is paper-only, drift untested (W.3 judge self-note, confirmed by 4/5 auditors). Confined today (lists identical); go-live blocker. | C.011.1 (fold into P0.W.5 worker edit) — import the constant + assert the SDK allowlist `===` `WORKER_ALLOWED_TOOLS` |
| A.011.2 | **High** | RFC PR 008 (`:450`), PR 014 (`:563`), §4.1 (`:320`) still name `learnings/SKILLS/seo-copywriter/*`; DR-022 moved canonical source to `skills/seo-copywriter-skill-package/`. Literal blocker on P0.W.5. Also PR 008's golden path `apps/seo/golden/*.json` should cite `examples/whispering-willows-demo/`. | spec-update (reconcile RFC to DR-022, in the P0.W.5 run) |
| A.011.3 | Med | `modelToolAllowlist` boot-refusal path never exercised via `launchSandbox` (only the pure helper unit-tested). | fold into C.011.1 (add launchSandbox boot-refusal test) |
| A.011.4 | Med | No `agent-worker.test.ts`/`emit.test.ts` — `runAgentLoop` wedge-timeout→terminal-error→lease-release (acceptance #4) + `emitFromSdkMessage` untested. | C.011.x or fold into worker hardening |
| A.011.5 | Med | `no-console` disable directives reference an unconfigured rule (dead no-ops in entry.ts/_harness.ts) + 7 unguarded `console.*` in the 4 routes — pick a posture (audit-001 A.012.x carryover). | C.011.x (config + logger or delete dead directives) |
| A.011.6 | Med | FSM emits `NOT_PUBLISH_VERDICT`; PRD §9.1 canonical table = `VERDICT_NOT_PUBLISH`. Transposed stable enum the worker/API/CI branch on. | code-change (rename in lifecycle-fsm.ts + tests) |
| A.011.7 | Med | `publish/route.ts:196` infers `evalRan` from `verdict !== null`, not a persisted `gate_results` row (DR-009 open item, ER-4 fail-open class). Safe today (vetoed verdict still blocks) but loose. | bind to `gate_results.eval_ran` at P0.S.2/PR 009; promote DR-009 open bullet to a DR |
| A.011.8 | Med | `packages/schema-flywheel` `test` script globs an empty dir → passes vacuously; the security-critical migrations are only covered transitively by the apps/seo RLS test. | patch (remove misleading script or add a thin schema test) |
| A.011.9 | Med | Worker `Dockerfile` doesn't COPY the suite tree; `agent-worker.ts` uses `settingSources:["project"]`+`skills:["seo-blog-writer"]` (the `~/.claude` mechanism DR-022 rejects). Latent until P0.W.5. | fix at P0.W.5 (COPY suite + load-suite resolves vendored path) |
| A.011.10 | Med | STATE.md overstated P0.W.5 block — DR-022 already resolves vendoring + golden source; only expert label *certification* is residual. | reconciled in this audit's STATE update |
| A.011.11 | Med | Unfiled DR for the run-006 "RLS-enabled-zero-policy v1 (anon=published-only, workspace-scoped policies deferred)" posture. | → DR-023 (filed this run) |
| A.011.12 | Low | Worker-credential publish scope: a bridge-JWT request can POST `/content/api/publish`; only the FSM release-gate blocks (sound, load-bearing) — capability-profile "no publish tool" is tool-layer only. Defense-in-depth note. | active risk + draft structured check `worker-credential-publish-scope` |
| A.011.13 | Low | `verifyBridgeToken` ignores header `alg` (NOT exploitable — always HMACs with private secret; `alg:none` fails length guard). RFC-7518 hygiene. | one-line `if (alg!=="HS256") reject` in a hardening pass |
| A.011.14 | Low | host-side `session-store.ts` lives in `worker/` dir (server-only, not bundled — mislocation smell); brief-route `assertTenancyMatch` asymmetry undocumented; contract-version pin still test-local (DR-009 deadline passed); event-log key-order cosmetic drift (runs 1-8 vs 9-10). | log / opportunistic |

## Positive confirmations (no finding)
- **MERGED-PR integrity:** 9/9 spot-checked MERGED + merge commits ancestors of origin/preview. No fabricated merges.
- **Tests:** zero hollow tests; zero unjustified skips; zero flaky tests. The #19/#20/#22 suites assert real behavior (boot refusals, dropped events, 401/403, ownership-spy-never-called proving token-as-credential).
- **RLS Tier-2:** audit-001's perpetual-skip orphan is CLOSED — 8 `[T2]` tests now EXECUTE in CI (live Supabase branch), 17/17 green.
- **Module boundaries:** `@sagemark/core` imports nothing from app/feature; worker never touches Supabase; vendored suite stays out of the build graph (not a pnpm workspace member).
- **Anchors:** §11.3/§11.4 (capability denial + 5-layer tenancy, cross-tenant closed), §9 (fail-closed publish — canPublish AND-s 5 preconditions, client_signoff rejected), §4 (Stage-A ordered vetoes) all honored.
- **Judge calibration:** re-judge 0%, BLOCKED 0%, no declining slope. The "enforcement seam built-not-wired" theme (runs 007-009) is converging (DR-018 standing CI regression), not drifting.

## Draft structured checks (for manifest.judge_criteria — from state-historian item 4)
1. **`tool-allowlist-single-source`** — trigger: any PR touching `agent-worker.ts` allowedTools/tools OR `capability-profile.ts` WORKER_ALLOWED_TOOLS/MODEL_DISABLED_TOOLS. Forced output: "the model's runtime allowlist in agent-worker.ts is `<literal|import>`; if hardcoded, FAIL with the drift risk named." Cite agent-worker.ts:279 vs capability-profile.ts.
2. **`worker-credential-publish-scope`** — trigger: any PR touching publish/route.ts auth, authenticateBridgeRequest, or the FSM release-gate. Forced output: "can a via:'bridge' request reach the publish transition? name EVERY server-side gate not under worker control that blocks it; if the FSM gate is removed/loosened, FAIL."

---

## Five auditors — verbatim summaries

### 1. architecture-auditor
0 Critical · 1 High (A.011.1 allowlist single-source paper-only) · 2 Med (A.011.3 modelToolAllowlist boot-refusal untested; A.011.4 no agent-worker/emit tests) · 2 Low (session-store mislocated in worker/; brief assertTenancyMatch asymmetry). Module boundaries clean; all 4 routes use authenticateBridgeRequest uniformly; vendored suite correctly out of build graph. Note: `apps/seo/src/lib/{release,byline}` dirs don't exist (logic inlined in publish/route.ts + context.ts).

### 2. convention-auditor
0 Critical/High · 2 Med (A.011.5 no-console dead directives + unguarded routes; A.011.1 allowlist single-source) · 1 Low (apps/seo/.gitignore was uncommitted — since committed fc9566d, stale). PASS: commit-message convention across #13-24; disciplined `any` (behind justified disables at SDK-interop only); `server-only` correctly placed; no secrets; tables correctly named; vendored package clean (no secrets/node_modules/tracked pyc).

### 3. spec-reconciler
F1 High (A.011.2 RFC stale suite path vs DR-022 — blocks P0.W.5) · F4 Med (A.011.6 NOT_PUBLISH_VERDICT vs §9.1 VERDICT_NOT_PUBLISH) · F5 Med (A.011.7 evalRan looseness, DR-009 open) · F2 Med (A.011.9 Dockerfile no COPY suite) · F6 Low (DR-016 RFC §2 text stale but invariant held) · F7 Low (DR-020 intra-tenant replay, cross-tenant closed) · F8 Low (contract pin test-local past PR 006 deadline) · F3 Low (load-suite.ts not yet existing — correct, PR 008 NOT_STARTED). Anchors clean (no violations).

### 4. test-quality-auditor
0 hollow, 0 dishonest skips, 0 flakes. F1 High (A.011.1 allowlist drift untested) · F2 Med (A.011.8 schema-flywheel vacuous test script) · F3 Info (RLS Tier-2 orphan RESOLVED — now executing) · F4 Info (no flakes; RLS fail→pass history is real fixes). Orphaned Tier-3 NEEDS-INPUT (tracked, now newly-runnable given live infra): live-Sandbox adversarial re-run, live worker session round-trip, live SSE e2e, OQ-3 Stage-B weight magnitudes — recommend consolidating 1-3 into a tracked "live-infra verification pass."

### 5. state-historian
MERGED integrity PASS (9/9). Decision-log: 1 gap (A.011.11 → DR-023 RLS-zero-policy). Calibration HEALTHY. Adversarial re-judge: (a) High A.011.1 allowlist disconnected from verified profile; (b) High A.011.3 modelToolAllowlist boot-refusal not e2e via the loop; (c) Med A.011.12 worker-credential publish scope (FSM gate is the real barrier — defense-in-depth); (d) Low A.011.13 alg header unchecked (downgraded from a parallel reviewer's CRITICAL — NOT exploitable). Event-log reconciles (1 cosmetic key-order drift). Cross-cutting: A.011.10 STATE stale vs DR-022.

*Consolidated by /seo-creator-build · audit-002 · 2026-06-26*
