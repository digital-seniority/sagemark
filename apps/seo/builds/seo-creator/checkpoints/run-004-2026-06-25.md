# Run #004 — IN_PROGRESS

**Started:** 2026-06-25T20:30Z · Mode: auto (iteration 4/8) · Branch: preview

## Plan
Single eligible PR: **P0.E.4** (PR 005 — /content/api/{brief,draft,audit,publish} kernel route contract). engine-port, Medium risk (the enforcement boundary). deps [P0.E.3 ✓, P0.S.1 ✓]. The LAST autonomously-reachable PR — after it the loop depletes (worker lane gated on P0.W.1).

## Write-scope
apps/seo/src/app/content/api/{brief,draft,audit,publish}/route.ts, apps/seo/src/lib/content/{serp-fetch,context}.ts, apps/seo/test/content/*.test.ts.

## Key constraints
- Port the route contract from flywheel-main origin/preview apps/agents/src/app/content/api/*.
- canPublish + Stage-A enforced HOST-SIDE in the routes (agent-unreachable); audit is READ-ONLY (no status mutation).
- SSRF guard on brief; tenancy 403 binding; voice-spec hard stop; kernel-host-unreachable hard fail; source-quality authority classes (a/b/c); class-(b)/(c) cannot satisfy a YMYL medical claim's VETO_UNSOURCED_STAT; contract-version build-fail.
- No Supabase wired → mock data layer or local Docker pg; Next.js routes → read node_modules/next/dist/docs (AGENTS.md).

## Status: agent spawning

---
## FINAL — Run #004 complete (loop terminal: depleted)

| PR | Lane | Judge | Status | Evidence |
|---|---|---|---|---|
| P0.E.4 | engine-port | APPROVED (P5/Pr5), GATE-BYPASS + TENANCY PASS | MERGED | PR #11 → ca776f0; 74 Tier-1 tests; audit read-only by type; canPublish host-side; YMYL trust floor |
| C.004.1 | engine-port (corrective) | orchestrator-verified (build green + 481 core tests) | MERGED | PR #10 → 269a0b1; core strict-clean; behavior-preserving |

Build-integrity finding: apps/seo build was RED on preview (DR-004 strict-vs-relaxed tsconfig mismatch in core; the per-lane judges only ran the package build, not the consuming app's). Fixed by C.004.1 + DR-008 (judges must run the integration build; orchestrator pnpm install after dep-changing merges).

Decisions: DR-008 (source-consumed build integrity, supersedes part of DR-004), DR-009 (audit read-only divergence).

State after: Phase 0 = 5/11 merged (P0.E.1, P0.E.2, P0.E.3, P0.E.4, P0.S.1). **LOOP DEPLETED** — every remaining PR (P0.S.2, P0.W.2-5, all Phase 1) transitively needs the worker-runtime lane, gated on P0.W.1's live Vercel Sandbox run (human). Self-verify: PASS.
