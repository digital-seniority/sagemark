# Run #010 — 2026-06-26

**Mode:** corrective (user-directed) · **Duration:** ~25 min · **Trigger:** user picked "Do the DR-018 corrective" at the Run #009→#010 fork (P0.W.5 blocked on the human-labeled golden corpus).

## Single agent — C.009.1 (DR-018 bridge-token enforcement)

| PR | Lane | Worktree | Result |
|---|---|---|---|
| C.009.1 | cross-cutting (worker-runtime + engine-port) | agent-ac029ff6ad9011dc0 | COMPLETE → judge APPROVED **5/5·5/5** → [PR #22](https://github.com/digital-seniority/sagemark/pull/22) → MERGED `2128791` |

## C.009.1 — enforce the per-run bridge JWT in every /content/api host tool (PR #22)
Discharges [[DR-018]]: PR 007 minted a per-run HS256 bridge JWT and the worker sends it on every kernel call, but no route verified it (the #1 agency-ending-risk seam the PR 007 judge flagged). This wires the verifier into **every** host tool:
- Extracted mint/verify primitives to NEW `apps/seo/src/lib/auth/bridge-token.ts` (constant-time sig check preserved); `api/run/route.ts` re-exports them → PR 007 tests stay green.
- NEW `authenticateBridgeRequest()` in `context.ts`: Bearer present → token is the credential (verify sig+expiry against the token's own claims, tenancy from claims, **reject if body.clientId ≠ token.cl** → no scope-widening); no Bearer → existing operator session path **byte-for-byte unchanged**.
- All four `/content/api/{brief,draft,audit,publish}` routes authenticate before any state touch.
- NEW standing **table-driven** regression `apps/seo/test/content/bridge-auth.test.ts` (4 routes × 6 cases: valid/expired/cross-run/cross-tenant/tampered/body-mismatch) — fails CI until every host tool authenticates.

**Judge APPROVED 5/5·5/5** ("exemplary corrective hygiene"). Security soundness rigorously verified: signature gate is independent of the `expected` scope so the self-claims pattern doesn't weaken it; cross-tenant fully closed (token-derived tenancy + body-vs-token match before any data access on all 4 routes); fail-closed (no fall-through to operator path on a bad token); `bridgeSigningSecret()` throws if unset (no forgeable fallback). Independent verification GREEN: 177 vitest (12 files), tsc clean, `next build` GREEN. TENANCY/GATE-BYPASS/§17 PASS. No anchor contradiction.

## Merge disposition
Touches auth/tenant-isolation kernel routes (policy always-skip), but the user explicitly directed this corrective and had already authorized auto-merging the worker auth/tenancy class this session (Run #009). Auto-merged (no re-ask). Squash → `2128791`.

## DRs recorded
- **DR-020** — bridge-token intra-tenant run scoping: kernel routes verify the token against its own claims (cross-tenant fully closed; intra-tenant same-window run replay is by-design within ~90s, to be hardened when a run registry exists per [[DR-017]]). The test's "cross-run" case = tamper-detection.
- **DR-021** — `authenticateBridgeRequest` two-function API: the standing pattern is routes call `authenticateBridgeRequest` (handles worker-bridge + operator), delegating to the unchanged `bindRequestContext` for the no-bearer path.

## Quality
- **Process:** 5.0/5 (surgical 8-file scope, verbatim extraction preserving PR 007's surface, all gates green, deviations disclosed, standing regression added).
- **Product:** 5.0/5 (closes the #1 agency-ending risk; fail-closed at every host tool; no forgeable secret; future un-authenticated route can't escape the table-driven test).

## State after
- **8/23** engineering PRs merged (unchanged) · **+3 correctives** (C.004.1, C.008.1, **C.009.1**) · +1 spike · 4 audit fixes.
- DR-018 **discharged**; the worker→host bridge is now authenticated end-to-end.
- **P0.W.5 (PR 008) still blocked** on the human-labeled Whispering Willows golden corpus (non-engineering) + the suite-skill→Sandbox delivery decision. No mapped autonomous engineering work remains.
- Runs since last audit: **5** (C.009.1 is a corrective; counting it, the audit gate is now AT threshold — **`/seo-creator-build audit full` is due before the next work-doing run**).

## Self-verification: ✅ (PR #22 MERGED on GitHub, mergeCommit ancestor of origin/preview; git status clean)

## Loop: NOT re-armed (user picked a discrete corrective, not "resume auto"). Next: audit due; P0.W.5 needs the golden corpus.
