# Run #008 — 2026-06-26

**Mode:** work-doing (1 high-risk PR) · **Duration:** ~35 min · **Trigger:** "merge them and continue" — merged the 4 audit PRs, then built the marquee worker host now that it's unblocked.

## Pre-run: merged audit fixes (user-approved)
PRs **#13** (A.005.1 RLS, `245eb65`), **#14** (A.005.2 spec, `b3e1d49`), **#15** (A.005.3 gates→Gateway, `186cb19`), **#16** (A.005.4 CI, `454ceaa`) squash-merged into preview. This unblocked P0.W.2 (spec reconciled, tenancy leak closed).

## Agent
| PR | Lane | Worktree | Result |
|---|---|---|---|
| P0.W.2 | worker-runtime | `agent-adf45e3d320ad0639` | COMPLETE → judge 5/5·4/5 → [PR #17](https://github.com/digital-seniority/sagemark/pull/17) |

## P0.W.2 — Agent-SDK worker on Vercel Sandbox (PR #17)
The build's highest-risk PR. Stands up `apps/seo/src/worker/{agent-worker,sandbox-launch,host-tool-bridge,session-store,entry}.ts` + Dockerfile, porting the **proven spike controls** into the real `sandbox-launch` (DR-010 egress + in-VM MMDS DROP; DR-011 no-shell workdir-scoped tool; `assertControlsOrRefuse` fail-closed boot). Adds `0034_worker_sessions.sql` + `workerSessions` drizzle def; deps `@anthropic-ai/claude-agent-sdk`, `@vercel/sandbox`, `@supabase/supabase-js`.

**Judge 5/5·4/5 — safety boundary provably fail-closed:**
- **GATE-BYPASS PASS** — model has no publish tool (`tools:[]` + 2-item allowlist; only `persistPiece`→`/content/api/draft` + `readWorkdirFile`).
- **TENANCY PASS** — frozen per-run `(workspace,client,run)` binding; model can't supply tenancy; host re-validates (403).
- **No ambient secrets** — worker env scrub refuses boot on `SUPABASE_SERVICE_ROLE_KEY`/`DATABASE_URL`/`ANTHROPIC_API_KEY`; persistence is host-side (`session-store` holds the service-role; the Sandbox has none).
- **Fail-closed boot** — applies the profile + `assertControlsOrRefuse` → refuses + tears down on any missing control (tests: MMDS-fail / allow-all / env-leak → BootRefusedError).
- **Gateway-only** — worker holds no raw provider key; CLI env seam (`ANTHROPIC_BASE_URL`+`ANTHROPIC_AUTH_TOKEN`) per **DR-016**.
- Tier-1 GREEN (102 vitest + 9 RLS-contract); SOURCE-CONSUMED build GREEN. Tier-2/3 (live Sandbox + Supabase round-trip) = **NEEDS-INPUT**, not fabricated.

**Product 4/5:** host lease-reclaim watchdog is library-built but not wired into a running orchestrator (acceptance #4 end-to-end deferred — **DR-017**); §C17 via env seam not the named `resolveGatewayModel('worker')` (**DR-016**). Both defensible at this thinnest slice.

## DRs recorded
- **DR-016** — worker model traffic via the Agent-SDK CLI env seam (Gateway-only invariant preserved + double-guarded).
- **DR-017** — host lease-reclaim watchdog / warm-pool manager deferred to the host-orchestrator PR; P0.W.2 ships the fail-closed primitives.

## State after
- 5/23 engineering merged (+1 corrective) · 4 audit fixes merged (#13–#16) · P0.W.1 spike merged · **P0.W.2 (#17) OPEN, human-merge.**
- Runs since last audit: 3 (audit due before Run #010).

## Terminal reason: REQUIRES_HUMAN_MERGE (hard-stop)
P0.W.2 is production-critical → human-merge by policy; **P0.W.3/W.4/W.5 gate on it merging.** Human actions:
1. **Review + merge PR #17** (the worker host).
2. **Apply `0034_worker_sessions.sql`** to the Sagemark project (`rilaycjkksfosnxvenzt`) — DB write, needs approval.
3. **Add `worker_sessions` to the RLS contract test** (PR-004-owned) so its fail-closed RLS is regression-guarded.
4. **Provision live Vercel-Sandbox + Supabase** to run P0.W.2's Tier-2/3 before the worker goes near a paying tenant.
5. **Set the CI `DATABASE_URL` secret** (Sagemark) for RLS Tier-2.
6. **Decide DR-013** host-context metering policy before the D4 ledger.

**Next-build candidate without waiting:** P0.S.2 (PR 009 — voice-spec hard stop + fail-closed publish) if its deps are worker-independent (deps P0.E.4 ✓ + P0.S.1 ✓).
