# Run #013 — 2026-06-26 — P0.S.2 (PHASE 0 COMPLETE)

**Mode:** work-doing (1 PR), auto-loop unattended (~1.5h into the 10h window). **Trigger:** P0.S.2 eligible after P0.W.5 (#26).

## Agent — P0.S.2 (PR 009)
| PR | Lane | Worktree | Result |
|---|---|---|---|
| P0.S.2 | schema-tenancy | agent-af18d0c4235a8f9af | COMPLETE → judge APPROVED 5/5·5/5 → [PR #28](https://github.com/digital-seniority/sagemark/pull/28) → MERGED `ea0fc0f` |

## P0.S.2 — voice-spec hard stop + fail-closed publish (PR #28)
Closes the inherited YMYL byline-trust hole — the thinnest-slice close-out. Voice-spec `approved_at IS NULL` hard stop (server 409, no default voice); fail-closed `POST /api/publish` driving `@sagemark/core` `canPublish` (NOT forked); `credentialed_releases` as the only human-release source (a `client_signoff` can NEVER satisfy → `NO_HUMAN_RELEASE`); byline server-resolved from the credential snapshot + persisted `author_id` (forged `request.author` rejected by `.strict()`); revoked/expired/inactive/dangling `byline_authorizations` blocks; no autopilot (PUBLISH verdict alone stays `draft`). Release/byline logic extracted into `src/lib/{byline,release}` and shared by both publish routes (no fork). 16-row truth table.

**Folded audit-002:** A.011.6 (FSM enum → `VERDICT_NOT_PUBLISH` per PRD §9.1; grep-clean) + A.011.7 (`evalRan` ← persisted `gate_results.eval_ran` via a fail-closed `getGateResult` seam).

**Judge APPROVED 5/5·5/5.** GATE-BYPASS / FAIL-CLOSED-PUBLISH / TENANCY / **SOURCE-CONSUMED BUILD** all PASS. core 479 + seo 233 tests, tsc + `next build` GREEN. No default-allow or autopilot path. Improvements (non-blocking): widen `PersistedAuthorization` projection (granted_at/scope) when wired; fixture `getGateResult` defaults evalRan:true.

## DRs recorded
- **DR-025** — `gate_results` table deferred to PR 020; `evalRan` seam is fixture-injected + production-throws (fail-closed) until then; PR 020 must wire the table + widen the authorization projection. Closes DR-009's `evalRan` open bullet.

## ★ PHASE 0 — FOUNDATIONS: COMPLETE ★
All 10 Phase-0 PRs MERGED: **P0.W.1** spike (#3) · **P0.E.1** (#2) · **P0.E.2** (#5) · **P0.E.3** (#8) · **P0.S.1** (#6) · **P0.E.4** (#11) · **P0.W.2** (#17) · **P0.W.3** (#19) · **P0.W.4** (#20) · **P0.W.5** (#26) · **P0.S.2** (#28). Plus 3 correctives (C.004.1/C.008.1/C.009.1), 4 audit fixes (#13–16), the vendored suite (#24), and 2 audits (001, 002).

**Phase-0 DoD status:** the ported deterministic kernel (gate/scorers/FSM), the multi-tenant schema + fail-closed RLS (17/17 CI), the `/content/api/*` kernel route contract, the Agent-SDK worker (hardened capability-denial profile + SSE transport + per-run bridge-JWT enforced end-to-end), the seo-blog-writer suite wired + golden-set regression harness, and the voice-spec-hard-stop + fail-closed publish are all MERGED. Golden corpus captured (honest baseline). **Residual (not blocking phase close, tracked):** live-Sandbox Tier-2/3 verification, expert golden-label certification, `apps/seo/dist` worker-build wiring, the demo-prose em-dash gate tension.

## Next: phase-close audit (required at boundary) → then Phase 1 under the unattended mandate.

## Self-verification: ✅ (#28 MERGED, ancestor of origin/preview; worktree clean)
