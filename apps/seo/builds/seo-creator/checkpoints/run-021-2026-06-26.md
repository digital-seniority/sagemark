# Run #021 — 2026-06-26 — P1.R.3 (homepage + hero + DR-033 gate)

**Mode:** work-doing (1 PR, render-geo + core), user-directed (the "do it" plan's final step).

| PR | Lane | Result |
|---|---|---|
| P1.R.3 (PR 017 — resource-library homepage + imagegen hero + DR-033) | render-geo | judge APPROVED 5/5·5/5 → [#47](https://github.com/digital-seniority/sagemark/pull/47) MERGED `cd5a49c` |

Resource-library homepage off the `clusterRole`/`funnelStage` first-class columns (SSR, published-only, tenancy by blog_slug). `[photo:]` hero resolution via in-process `@sagemark/imagegen` `generateHeroImage` — async/job-wrapped (SSR reads PERSISTED assets, never blocks on generation), Pexels-first then generated, `IMAGEGEN_LIVE`-gated OFF, fail-closed `makeNotWiredImageStore` → degrade to placeholder (not 500). **DR-033 publish-side image-license gate IMPLEMENTED:** `TransitionContext.referencedImages` + `canPublish` blocks `UNLICENSED_ASSET` for any orphaned/unlicensed referenced image — fail-closed including when the resolver is absent (tokens present + no resolver → block); render refuses unprovenanced assets. Judge: DR-033-license-gate / tenancy / spend-safety / source-consumed-build all PASS; core 489 + seo 408 tests + build green.

## DR-033 → IMPLEMENTED (the gate landed; live Drizzle seam-resolver wiring is the follow-up).
## Quality: Process 5.0 · Product 5.0 · re-judge 0%.
## State after: 18/23 engineering (Phase 1: 8/12 — P1.R.1/R.2/R.3/W.1/U.1/U.2/U.3/U.4). imagegen built out (S1+S2, applied). Runs since audit: 1 (audit-004).
## Next: P1.C.1 (PR 018 client-review preview, ←P1.R.3 ✓). Follow-ups: wire the live resolveReferencedAssets/resolveHeroAssets Drizzle impls (so image-bearing pieces publish); F1 edit status-guard; promote the 3 structured judge checks; imagegen live-flip (IMAGEGEN_LIVE + creds + model-id smoke). NON-ENG: P1.C.2 (D6 reviewer), P1.C.3/4 (≥3-engine SoM).
## Self-verification: ✅ (#47 MERGED, ancestor of origin/preview; worktree clean).
