{
  "name": "sagemark",
  "version": "0.1.0",
  "private": true,
  "description": "Marketing OS for the retirement home industry",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "turbo": "^2.5.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
